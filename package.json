{
  "name": "aramco-reviews-enterprise",
  "version": "1.0.0",
  "description": "Advanced Aramco Reviews App for collecting, moderating, analyzing and acting on customer feedback",
  "private": true,
  "scripts": {
    "setup": "npm run setup:backend && npm run setup:frontend && npm run setup:workers",
    "setup:backend": "cd backend && npm install --legacy-peer-deps",
    "setup:frontend": "cd frontend && npm install",
    "setup:workers": "cd workers && npm install",
    "dev": "npm run dev:backend & npm run dev:frontend & npm run dev:workers",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:workers": "cd workers && npm run start",
    "build": "npm run build:backend && npm run build:frontend && npm run build:workers",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:workers": "cd workers && npm run build",
    "test": "npm run test:backend && npm run test:frontend && npm run test:workers",
    "test:backend": "cd backend && npm run test",
    "test:frontend": "cd frontend && npm run test",
    "test:workers": "cd workers && npm run test",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "cd backend && npm run lint",
    "lint:frontend": "cd frontend && npm run lint",
    "format": "prettier --write .",
    "docker:build": "docker-compose -f infrastructure/docker-compose.yml build",
    "docker:up": "docker-compose -f infrastructure/docker-compose.yml up -d",
    "docker:down": "docker-compose -f infrastructure/docker-compose.yml down"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
